/*jshint esversion: 6 */
// @ts-check

let crate = 0;
// A playable character
/**
 * @typedef CrateProperties
 * @type {object}
 * @property {number} [x=0]
 * @property {number} [y=0]
 * @property {number} [randWeapon=0]
 */
export class Crate {
    /**
     * @param {CrateProperties} params
     */
    constructor(params = {}) {
        
        // Create list of weapons
    }

}